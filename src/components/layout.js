import React from "react"

export default ({ children }) => (
	<div
		style={{
			margin: `3rem auto`,
			width: "100%",
			// maxWidth: "80vw",
			// padding: `0 1rem`,
		}}
	>
		{children}
	</div>
)
